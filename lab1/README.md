# Football Analysis Database GUI

Приложение для работы с базой данных футбольной статистики UEFA Champions League 2016-2022 с графическим интерфейсом для анализа данных.

## Описание

Этот проект предоставляет удобный интерфейс для подключения к PostgreSQL базе данных с футбольной статистикой, выбора данных из различных таблиц и проведения анализа. Приложение позволяет:

- Подключаться к базе данных PostgreSQL
- Просматривать таблицы и их колонки
- Собирать данные из нескольких таблиц с автоматическим построением JOIN
- Выполнять одномерный анализ с построением гистограмм
- Проводить многомерный анализ с построением диаграмм рассеяния
- Сохранять результаты в CSV-файлы


## Требования

- Python 3.10.5
- PostgreSQL 15+
- Docker и Docker Compose (опционально, для запуска базы данных)

Возможно, будет работать и на других версиях, но я не ручаюсь за это.

## Установка и запуск

### 1. Запуск базы данных


```bash
docker-compose up -d

```

База данных будет доступна на `localhost:5433` с параметрами:
- БД: `football_db`
- Пользователь: `admin`
- Пароль: `admin`

### 2. Установка Python зависимостей

```bash
pip install -r requirements.txt
```


### 3. Инициализация базы данных

#### 3.1. Подготовка данных

Поместите файл `UEFA Champions League 2016-2022 Data 3.xlsx` в корневую папку проекта. 
Найти его можно по [тут]((https://www.kaggle.com/datasets/cbxkgl/uefa-champions-league-2016-2022-data)) 

#### 3.2. Создание таблиц

```bash
python excel_to_alembic.py

alembic init migrations

alembic upgrade head
```

Возможно, придется пофиксить импорты 🥰

### 4. Запуск приложения

```bash
python app.py
```

## Использование приложения

### Подключение к базе данных

1. В главном окне введите параметры подключения:
   - Host: `localhost`
   - Port: `5433` (или стандартный порт PostgreSQL)
   - Database: `football_db`
   - User: `admin`
   - Password: `admin`

2. Нажмите кнопку "Connect"

### Выбор данных

1. В списке таблиц выберите нужную таблицу
2. В списке колонок отметьте галочками нужные поля
3. Нажмите "Добавить выбранные колонки в итог"
4. Повторите для других таблиц при необходимости

### Сбор данных

1. Убедитесь, что опция "Apply automatic JOIN map (recommended)" включена
2. Нажмите "Build DataFrame"
3. Данные отобразятся в таблице справа

### Анализ данных

#### Одномерный анализ (гистограммы)

1. Соберите DataFrame
2. Нажмите "One-dimensional analysis (histograms)"
3. Выберите числовые колонки для построения гистограмм
4. Нажмите OK для отображения графиков

#### Многомерный анализ (диаграммы рассеяния)

1. Соберите DataFrame
2. Нажмите "Multivariate analysis (scatter)"
3. Выберите колонки для осей X и Y
4. При необходимости включите отображение линии регрессии
5. Нажмите OK для отображения графика

### Сохранение данных

1. Нажмите "Save CSV"
2. Выберите путь и имя файла
3. Данные будут сохранены в CSV формате

## Структура базы данных

База данных содержит следующие таблицы:

- `stadiums` - информация о стадионах
- `teams` - информация о командах
- `players` - информация о игроках
- `managers` - информация о тренерах
- `matches` - информация о матчах
- `goals` - информация о забитых голах

## Примечание

- Приложение автоматически строит JOIN между таблицами на основе предопределенных связей. Скорость не гарантирована
